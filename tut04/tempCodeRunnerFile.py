 # if words[0]=="rollno":
            #     temp=initial
            # else:
            #     roll_no=words[0]
            #     check=os.path.join(direc,roll_no+".csv")
            #     #checking if file already exist or not
            #     if os.path.isfile(check) is True:
            #         with open(check,"a") as g:
            #             g.write(initial)
            #     else:
            #         with open(check,"w",newline="") as g:
            #             writer=csv.writer(g)
            #             g.writerow(temp)
            #             g.writerow(initial)